#pragma once
struct Stats;
struct Affinities;
struct Attributes;
namespace SkillFunctions
{
	int Attack(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int IDidNotHitHer(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Oh(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int HiMark(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int HowIsYourSexLife(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int MegalomaniumSlap(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int DiosJuzga(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int YoSeLosMando(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SilverRain(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int PinchesFurros(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LlamarAlComandante(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int VanishToFurZone(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int BanHammer(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int BanHammerConquest7890099(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int JerichoPunisher(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int UCantCM(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Rko(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ManitaDePuerco(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int GarfiasSmash(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Urracarrana(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Suplex(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SuperSuplex(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LaVoladora(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int PanYCirco(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int AlburDeAlbañil(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int AlburCentralDeAbastos(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int AlburCTM(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Pierrotaso(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SantoTiesoDelMercado(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Sillaso(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LaQuebradora(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int KneeReaper(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int EchoArrow(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int FireFirePiwPiwPiw(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int OilSearch(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int GunsBan(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int HolyGranade(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int RedneckStuff(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LasQueTruenan(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LasQueTruenanResio(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int MataPolicias(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int MataYisus(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SPFKStealth(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SaintBulletOfShrekKomrade(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LaMataThots(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LaMataThotsResia(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ThotSlayer(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int BegoneThot(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int DeusVult(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ThotDestroyer(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ThotDetector(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ThotomicBomb(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int HolyWaifuBullets(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int BrosBeforeHoes(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int CheaseeFingers(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int FerrumMagnet(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int LaEstafaMaestra(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int SaladFingerKnife(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Nou(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Urmom(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int YElReinoDeSiriaSeraDestruido(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int HellBanishes(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Hipopotomonstrosesquipedialofobia(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int AnywhareNearSiria(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Esta(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int GreatGigInTheSky(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Bunchi(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Aseitgiantnreptomorfusilard(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Trakatula(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int DeathKnugget(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int TurboDoge(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Slime(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int Snoopdog(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ReclutaSFPK(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int ElSanto(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
	int IIiIIL(Stats stats, size_t skillPoints, Affinities targetAffin, Attributes sourceAttrib);
}